# RallyPath v0.2 — 現行功能與驗收邊界

更新：2026-09-19。這是已發布 v0.2.0 的現行規格與驗收邊界；正式基準為 main 的 `9158a2b`。
歷史方案保留在 Git 歷史與 docs/archive；不再把所有舊分支當作待開發清單。

## 核心流程

首頁：豎向最新畫板預覽 → 接著畫／畫第一拍；「想下一分」「記下剛才一分」共用同一編輯器。
上滑後顯示畫板歷史，依戰術、練習、比賽回顧篩選；知識庫只有一個「找個打法」入口。
模板只閱讀、播放或開啟不建立草稿；發生有效編輯才保存。練習用途不等於完整訓練管理系統。

## 畫板與場地

- 預設沉浸式直向全場。基線外保留縮短的 Defense 範圍，單打線、雙打線和基線不混為一談。
- 頂部：返回、固定 RallyPath 名稱、撤銷、重做、說明、更多。底部：添加、播放、刪除／拍次。
- 點選物件直接編輯；不恢復物件二級選單。取消選取後右下角顯示拍次入口。
- 線上小圖示切換直線／曲線，曲率拖點與按鈕不能重疊。
- 右下角刪除依選取內容變化；完整操作意義集中在說明。保留讀屏標籤及必要錯誤回饋。
- 更多使用上滑抽屜，高度最多 5/8；點畫板關閉。重新命名使用獨立原生鍵盤安全層。
- 場地主題 Hard／Clay／Grass、分區色塊和分區名稱置於更多選單底部；三者獨立，僅為本機顯示偏好。
- 分區半透明、位於場線和物件下方；名稱可關閉。場地主題不影響物理速度或存檔內容。

## 畫球路與球速

1. 從網球拖出球路，預設向畫面右側彎曲。
2. 落點穩定停留 0.8 秒後開始充能；未蓄力為 Control。
3. 充能依序 Control → Drive → Put away，達上限不循環。
4. 按住時沿 50% 透明球路顯示移動網球，沒有陰影；箭頭前僅顯示當前英文狀態。
5. 放手後上述臨時反饋消失；保存幾何、球速及衍生播放時長為一次可撤銷編輯。

充能啟動後門檻由 model 定義：Drive 250ms、Put away 700ms。
不再使用能量條、能量圈、跑馬燈光點或手動每拍秒數選單。
既有 duration 仍為播放來源，老草稿不強制推算／覆寫 pace。

## 球員、拍次與播放

- 新畫的球員跑位為直線，既有存檔曲線保留。
- 球路完成自動轉接球方跑位，接著繼續下一拍。
- 接球方跑動與對方擊球同步，不是球先到才開始移動。
- 自動產生的空白續拍不增加播放／匯出拍數；手動建立的空拍保留。
- 編輯、首頁預覽、GIF／影片使用相同的時序資料。
- 一鍵還原發球站位置於更多選單，可撤銷，不另設空白畫板體系。

## 存檔與相容

保留 localStorage 鍵 tennis-tactics:board-drafts:v1 與文件 version:1；產品 0.2.0 不等於存檔格式升版。
自動保存僅限目前瀏覽器；不提供帳號、雲端保存、多裝置同步。
可編輯備份為 JSON；PNG、GIF、影片是分享輸出，不可當作可編輯備份。
儲存損壞／配額失敗不得假報成功或覆蓋原檔。
原生輸入，不恢復模擬鍵盤、手機外框或固定 iPhone 寬度套用到 iPad。

## 本次不擴充

不重做首頁、不恢復舊抽屜首頁、物件選单、訓練三卡。
舊 decorateDrillBoard／額外訓練預設留在歷史，未經產品確認不加入。
發布收尾不自動刪除歷史分支、不改寫歷史，也不改變使用者本機存檔。
保留歷史決策／組合教學，不把其舊開發提案當作新需求。

## 驗收

執行 npm run verify:release；檢查球路連續性、直線跑位、同步播放、撤銷重做、模板不落草稿、
用途分類、還原、保存失敗、備份及媒體輸出、返回導航、原生鍵盤、發布子路徑。
iPhone／iPad 直橫向在 Chromium 與 WebKit 補充檢查；模擬測試不等同實機 Safari、手指／Pencil、
系統鍵盤和原生分享驗收。詳細結果見 docs/maintenance/V0_2_CONSOLIDATION.md。

發布狀態與正式站核對見 docs/maintenance/V0_2_RELEASE_CLOSEOUT.md；實機 iPad Safari、手指／Pencil 及原生分享仍須在對應硬件上記錄結果，不能以模擬矩陣代替。
