# RallyPath 当前迭代：速度表达与沉浸式球场显示

## 产品目标

不增加新的首页层级，继续强化战术画板本身：让青少年用一次连续手势表达“球打到哪里、这一拍有多主动”，并能用直觉图标查看站位区域和切换场地。

本迭代一次纳入以下能力，不再把球场显示延后到后续版本：

1. `Control / Drive / Put away` 三段蓄力。
2. 默认展示的站位分区色块，以及可选的区域名称。
3. 可在画板内直接切换的 Hard、Clay、Grass 三种球场主题。
4. 顶部与底部常驻操作全部收敛为图标，完整说明按需展开。

## 已确认：三段蓄力环

采用“落点三段环”方案，不再弹出三选一菜单。

- 连续手势固定为：从网球拖出球路 → 落点停稳 → 保持不放蓄力 → 松手确定。
- 最低档为 `Control`，继续蓄力进入 `Drive`，最长蓄力进入 `Put away`。
- 画面只显示英文 `Control / Drive / Put away`，不显示中文释义、秒数或数值速度。
- 三档单向推进，到 `Put away` 后封顶，不循环。
- 当前档通过落点环段、网球光圈和球线脉冲共同反馈；震动只能作为支持设备上的辅助反馈。
- 手指重新移动超过容差后，解除落点锁定并继续调整落点；再次停稳后重新蓄力。
- 松手时把球路、档位和该拍时长作为一次操作保存，一次撤销即可完整恢复。
- 提交后继续现有智慧回合：自动进入接球方跑位，球与同拍跑位保持同步。

首轮真机测试暂以“落点停稳约 150ms 后开始识别；约 0.25s 进入 Drive；约 0.7s 进入 Put away；约 1s 封顶”为校准起点。最终阈值以青少年能否稳定选中目标档位为准，不把测试数值展示给用户。

内部继续保留每拍 `duration` 作为播放真值。档位结合球路线实际长度换算时长，因此画板播放、首页预览、视频与 GIF 会保持一致。旧画板没有档位时继续沿用原时长，不自动改速。产品界面移除手动修改每拍秒数的入口，避免“档位”与“秒数”两套速度逻辑互相冲突。

## 当前迭代：球场显示

### 站位分区提示

参考用户提供的站位分区图，将球场按战术站位关系划分，而不是做成落点九宫格。每个半场采用镜像结构：

1. `Defense`：底线后的防守区域。
2. `Rally`：底线附近的相持区域，并保留中央相持区与两侧斜线通道的关系。
3. `Pressure`：进入底线内、开始压缩对方时间的区域。
4. `Attack`：底线至发球线之间的进攻区域。
5. `Net`：发球线至球网之间的网前与终结区域。

显示规则：

- 使用不同但低饱和的半透明色块提示区域，透明度随球场主题自适应。
- 分区位于球场底色之上、白线和所有战术对象之下；球员、球路、跑位和控制柄始终保持最高可读性。
- 分区色块默认展示，区域名称默认隐藏。需要学习站位时，可在右上角 `···` 中单独开启区域名称；关闭名称不会关闭色块。
- 球场上不长期铺大段文字，正常编辑与播放只保留场地、战术对象和图标控制。
- 分区只是学习辅助，不判断对错、不自动改变路线，也不影响拖动、命中或播放时间。
- `···` 中可分别控制分区色块与区域名称。色块默认开、名称默认关；两者都作为当前设备的显示偏好保存，不写入战术动作、草稿或模板数据。

### 球场主题

提供三种纯视觉主题：

- `Hard`：硬地，作为默认主题。
- `Clay`：红土。
- `Grass`：草地。

主题只改变球场底色、白线对比和极轻的表面纹理。红蓝球员、黄绿色球路、浅蓝虚线跑位继续使用固定的战术语义颜色，并通过白环或深色底描边确保在三种场地上都能辨认。

同一条路线与同一个 `Control / Drive / Put away` 在三种主题中必须拥有完全相同的时长。本迭代不模拟红土减速、草地低弹、摩擦、旋转或滑步；真实场地物理需在未来拥有完整弹跳模型后单独规划，不能用隐藏倍率代替。

主题通过顶栏的场地图标在画板内直接选择，不进入 `···` 二级菜单。所选主题与分区显示方式一样，是当前设备的显示偏好，不写入战术内容，也不触发草稿自动保存；本机预览与媒体渲染可读取当前显示偏好，但路线、球员、每拍档位和时长保持不变。

## 沉浸式控制

- 顶栏常驻按钮全部为图标：返回、撤销、场地主题、说明、更多。
- 底栏常驻按钮全部为图标，并随状态只显示当前有用的添加／调整、播放、历史／删除操作。
- 默认画板不展示操作提示、标签图例或持续占位的说明文字。
- 说明按钮打开完整操作说明，集中解释所有图标、拖画球路、落点蓄力、球员跑位、曲线调整、主题和历史记录。
- `Control / Drive / Put away` 是手势过程中短暂出现的状态反馈，不构成常驻文字菜单。

## 菜单逻辑

```text
顶栏
├─ [返回]
├─ [撤销]
├─ [场地图标]
│  ├─ Hard
│  ├─ Clay
│  └─ Grass
├─ [说明]
└─ [···]
   ├─ 站位分区色块  [开关，默认开]
   └─ 显示区域名称  [开关，默认关]

底栏
├─ [添加／调整]
├─ [播放]
└─ [历史／删除]
```

## 开发顺序

1. 完成三段蓄力状态，以及球路、pace、duration 的一次提交和一次撤销边界。
2. 将球场颜色抽成主题配置，加入 Hard、Clay、Grass，并验证球员与路线对比度。
3. 加入默认展示的只读站位分区层，以及色块和区域名称的独立显示开关。
4. 收敛顶部、底部图标控制并补全说明面板。
5. 验证编辑、首页预览和媒体导出共用同一套时长与渲染逻辑。
6. 在 320×700、390×844、iPhone Safari 与 Android Chrome 上完成手势、颜色、性能和导出检查。

## 验收标准

- 第一次看到蓄力反馈的使用者能说出：按住越久，依次为 Control、Drive、Put away。
- 拖动找落点的自然停顿不会误提交；继续移动可以立即恢复落点调整。
- 三档选择、球路、同拍跑位、首页预览和媒体导出使用同一时长。
- 分区开启或关闭都不会改变路线命中、曲线调整、自动保存或播放帧率。
- 首次进入画板时能看到分区色块但没有区域名称；从 `···` 开启名称后可立即看到，再关闭也不会改变战术内容。
- Hard、Clay、Grass 可从画板顶栏的场地图标直接切换，不需要进入 `···`。
- 顶部与底部常驻控制没有文字标签；用户可通过说明按钮查看所有图标和手势含义。
- 红土上的红方、硬地上的蓝方与跑位线、草地上的黄绿色球路都能清楚辨认。
- 切换球场主题不会改变任何一拍的 `duration`。
- 一次撤销可同时还原刚提交的路线、pace 和 duration。

## 本轮不做

- 按压压力感应；网页只使用稳定可测的停留时长。
- 手动输入每拍秒数作为主要交互。
- 球场主题自动改变球速或弹跳。
- 根据站位分区给出答对／答错、得分或评分。
- 把主题选择放回 `···` 二级菜单，或在球场上长期展示主题文字。
- 把区域名称默认打开，或让大量操作说明长期占据球场空间。

## 后续保留方向

原先规划的“同一场上信号，多种战术选择”仍有产品价值，但不与本轮画板基础表达能力同时开发。待蓄力与球场显示稳定后，再选择一个组合战术验证“看场面 → 选打法 → 看收益与风险”。
